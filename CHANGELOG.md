# 版本紀錄

版本號採用 [語意化版本](https://semver.org/lang/zh-TW/)：`主版號.次版號.修訂號`。1.0 以前，次版號代表新功能或行為改變，修訂號代表修正。每次發布的最新版本寫在最上面，且必須和 `maf/__init__.py` 的 `__version__` 相同。

## [0.4.0] - 2026-09-27

依 YTDL 1080 C 組的對話紀錄：時間主要耗在「以為還在跑，其實已經停住」，以及主對話來回的輪數。

### 修正

- `work` 不再無聲空轉：沒有任何任務能自己往下走時會自動結束並說明，背景執行的主對話會收到完成通知。先前沒帶 `--run-id` 的 `work` 永遠不結束，主對話一直等不到通知。Herdr supervisor 改用新的 `--daemon`。

### 新功能

- `night --integrate`：鏈跑完後自動把全部通過的鏈 cherry-pick 到目前分支（衝突就跳過並還原），用所有任務的驗收指令驗證整合後的 commit，最後只印一份報告。
- `night` 開始前一次列出所有需要核准的任務；沒加 `--approve` 就不開始，不會半夜停在核准。

### 行為改變

- 試跑驗收指令時，要執行的檔案若由計畫中的任務建立，視為正常的尚未通過，不再判為無法執行。
- MAF skill 加入「最少主對話輪數」流程：一次問完、背景執行 `night --approve --integrate`、等通知、只讀一次報告、不查 `--help`。

## [0.3.0] - 2026-09-27

依 YTDL 1080 實驗結果（Opus 單獨做約 6 分鐘，拆成 11 件委派反而多用約 5 倍主對話額度）調整委派方式。

### 新功能

- `retry RUN_ID [--note 說明]`：帶著上次失敗的原因重排同一件小任務，原本等它的下游自動改接到新任務，舊任務標記為已取代、不再出現在報告的待處理清單。
- 計畫的介面約定會自動寫進每件任務的說明，重排或交給 coder 時不會遺失。
- Codex coder 交件前會先在自己的沙箱跑任務的驗收指令（MAF 事後仍會獨立再跑）。

### 行為改變

- MAF skill 與規畫者改為：主對話一次做得完就不拆；要拆就拆成少數大塊；失敗用 `retry`；跑完再看 `report`，不頻繁查進度。

## [0.2.2] - 2026-09-27

### 修正

- `night --plan` 不再重複排入同一份計畫：每個任務記錄來自哪份計畫，該計畫還有未結束的任務時會拒絕，並提示用 `report` 查看進度。

## [0.2.1] - 2026-09-27

### 修正

- 測試或 agent 結束後，清掉它們留下的子程序：每個測試指令跑完就清空它的程序群組，並清掉仍在任務工作樹裡執行的孤兒程序。先前 coder 自己跑測試時中斷，留下的測試用無限迴圈會一直佔用 CPU。
- `work --run-id`（沒加 `--once`）在指定的任務都結束或需要人處理時會自動退出，不再一直空轉；`night` 也改用同一套判斷。

## [0.2.0] - 2026-09-26

### 新功能

- **一晚跑一批**：`/maf-plan` 讓另一家的強模型寫出結構化計畫（介面約定、主對話先做的核心、需要你決定的問題、任務鏈），`decide` 記錄決定，`night --plan` 試跑驗收測試後依序執行，早上用 `report` 看中文報告。
- **任務依賴鏈**：`delegate --depends-on RUN_ID` 讓下一件從上一件測試通過的 commit 接著做；上游等額度時下游會等，上游需要重新規畫時下游不執行。
- **`quick-codex` flow**：小任務交給 Codex GPT-6 Luna（推理 `none`）；小任務 Agent 可以是 Pi、Antigravity 或 Codex。
- **Codex 主對話**：`codex-pi` flow 與 Codex 的 `$maf`、`$maf-plan`；規畫者一定和主對話不同家。
- **Herdr 工作流 pane**：每次工具呼叫一行，顯示工具與碰到的檔案，不顯示 prompt 或內容。
- **快取命中率**：`progress`、`handoff` 顯示每次 agent 呼叫的快取命中率。
- **推理強度基準測試**：`bench/effort/` 用固定題目比較各 flow 在不同推理強度下的成功率、速度、token 與成本。
- **動畫導覽網站**：GitHub Pages 上的互動示範。
- **版本機制**：`flow.py --version`、`doctor` 顯示版本，每個 run 記錄執行時的 MAF 版本。

### 行為改變

- 以「maf」開頭的話才是在指揮 MAF。
- Pi 與 Antigravity coder 預設推理強度改為 `low`；Antigravity 改用 `gemini-3.8-flash-low`。
- 推理強度不再列入訂閱確認範圍；換模型或工具仍需確認。
- 主對話提示詞精簡為只說何時、為什麼委派。

### 修正

- Antigravity 的模型 ID 與推理強度不一致時，儲存 flow 就會擋下。
- 文件中全形標點與粗體標記的顯示問題。

## [0.1.0] - 2026-09-23

- 第一個可用版本：主對話委派、獨立工作樹、綁定 commit 的測試與可選獨立審查、訂閱確認、Flow Studio、Herdr 觀看模式。
